<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="viewModel"
            type="com.elkcreek.rodneytressler.musicapp.ui.albumsview.AlbumsViewModel"/>

        <variable
            name="adapter"
            type="com.elkcreek.rodneytressler.musicapp.utils.Adapter"/>

        <import type="android.view.View"/>
        <import type="android.support.v7.widget.LinearLayoutManager"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:clickable="true"
        android:focusable="true"
        android:background="@android:color/black">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler_view_albums"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:data="@{viewModel.albumList}"
            android:adapter="@{adapter}"
            app:adapter="@{adapter}"
            app:layoutManager="android.support.v7.widget.LinearLayoutManager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginBottom="50dp">

        </android.support.v7.widget.RecyclerView>

        <TextView
            android:id="@+id/no_albums_text"
            android:visibility="@{viewModel.albumList.size() > 0 ? View.GONE : View.VISIBLE}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="32sp"
            android:text="@string/no_albums_available_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <FrameLayout
            android:id="@+id/loading_layout"
            android:visibility="@{viewModel.shouldShowLoadingLayout ? View.VISIBLE : View.GONE}"
            android:background="@android:color/black"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ProgressBar
                android:id="@+id/bio_progress_bar"
                android:layout_gravity="center"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

        </FrameLayout>

    </android.support.constraint.ConstraintLayout>
</layout>
